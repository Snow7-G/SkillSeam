# SkillSeam 项目文档

> 版本 v1.1（CLI + 网页全功能）· 2026-09-21 · 状态：待建仓发布
> 一句话：模拟 agent 挑选 skill 的真实过程，在你上线之前告诉你——**哪些任务会被接住、哪些会被别的 skill 截胡、改哪句 description 能修好**。

---

## 1. 它解决什么困难

Agent（Codex / Claude Code / Cursor 等）加载 skill 的方式是：**启动时只读每个 skill 的 name 和 description 那一两行字**，用户任务来了，靠这几行字决定加载哪个 skill。这带来三个测试环境发现不了、只有线上才炸的困难：

**困难一：单看每个 skill 都没问题，放在一起就打架。**
两个 skill 的 description 各自合理，但关键词互相重叠——用户问"我上周的视力报告怎么解读"，本该命中「报告解读」，却被 description 里写了"受理视力、报告相关咨询"的「挂号预约」截胡。截胡的 skill 接不住任务，开始瞎编。本项目 demo 实测（40 条任务）复现了这两类冲突，见 `examples/report-demo-qwen.html`。

**困难二：这种错误不报错。**
agent "安静地选错了"，没有异常、没有日志告警，测试环境全绿。等你从用户投诉里发现，已经默默错了几周。

**困难三：修了不知道有没有修好。**
改 description 全凭手感——写"仅当用户明确要求 XXX 时"这种句式有没有用？改完之后 agent 的选择行为变没变？没有工具能回答。

**SkillSeam 就是把「agent 到底会不会选它」这个问题，从线上搬到你的提交之前。** 原理很直接：把你的整套 skill 按真实注入格式（只有 name + description）喂给一个模型扮演选择器，对一批任务逐个跑选择，输出"预期 vs 实际"的偏差地图。

### 与现有工具的分层（不是竞争关系）

| 工具 | 管的事 | 粒度 |
|---|---|---|
| agnix | 格式规范（frontmatter、命名、长度） | 单文件 |
| skilltest | 单个 skill 单独放着能不能被触发 | 单 skill |
| NVIDIA SkillSpector | 安全（注入、外传、供应链） | 单 skill |
| **SkillSeam** | **组合后的选择行为：谁抢谁的活** | **整个 skill 集合** |

---

## 2. 快速开始

环境要求：Python 3.10+，零第三方依赖。

```bash
# 第 1 步：配置 provider（二选一）
# 方式 A：项目根目录放 .atlasrc.json（推荐，可 gitignore）
#   {"base_url": "https://xxx/v1", "api_key": "sk-...", "model": "模型名"}
# 方式 B：环境变量 DASHSCOPE_API_KEY 或 OPENAI_API_KEY（+可选 OPENAI_BASE_URL / ATLAS_MODEL）

# 第 2 步：跑（第一个参数是你的 skill 目录，每个子目录一个 SKILL.md）
python3 skill_seam.py ./demo-skills

# 第 3 步：看报告
open output/report.html
```

没有 API key？加 `--mock` 离线体验完整流程（确定性关键词打分，仅用于验证管线，结论不可信）。

**费用参考**：6 skills × 40 任务 × 5 次采样 = 200 次调用，flash 级模型约几分钱、2 分钟。

### 命令行参数

| 参数 | 说明 |
|---|---|
| `<skills 目录>`（位置参数） | skill 集合根目录，扫描其下 `*/SKILL.md`，默认 `./demo-skills` |
| `--tasks <file.json>` | 自定义任务清单（推荐：真实用户问法） |
| `--demo-tasks` | 使用内置 40 条眼科 demo 任务 |
| `--gen-positive <N>` | 自动生成时每 skill 的正向任务数（默认 5） |
| `--gray-pairs <K>` | 自动生成时挑选的灰区技能对数（默认 2） |
| `--mock` | 离线关键词打分模式 |

退出码：`0` 无冲突（可接 CI）· `1` 有稳定冲突 · `2` 配置/参数错误。

### 任务来源（三级优先级）

1. **`--tasks my-tasks.json`（推荐）**——真实用户问法的冲突检出效果最好；
2. `--demo-tasks`——内置 40 条眼科 demo 任务；
3. **默认（无参数）：自动生成**——每个 skill 由 LLM 生成正向问法；灰区对由 LLM 通读全部 description 挑出最易混淆的职责重叠对（失败回退 Dice 系数词面相似度），再为每对双向生成边界问法。生成结果落盘 `output/tasks-generated.json`，**建议人工审核后用 `--tasks` 复跑**。

> **实测警告（生成者偏见）**：同一个 demo skill 集，手写真实问法测出 4 个稳定冲突，LLM 自动生成的问法 0 个——因为生成任务时模型就知道"该谁赢"，问法意图天然清晰。**自动生成适用于冒烟测试和发现"该人工出题的重叠对"，冲突检测结论必须基于真实用户问法。**

### 自定义任务清单格式

```json
[
  {"t": "用户任务原文", "e": "应该命中的skill-name", "kind": "positive"},
  {"t": "语义交界的模糊任务", "e": "更符合用户意图的那个", "kind": "gray", "pair": "skillA↔skillB"}
]
```

- `positive`：意图清晰、理应命中的任务；`gray`：两 skill 语义交界的灰区任务（冲突高发区，建议占 20-30%）。
- `e` 必须是目录里真实存在的 skill name，否则工具报错退出（防呆）。

---

## 3. 怎么读报告

**总览卡片**：命中数 / 稳定冲突 / 不稳定。健康状态 = 命中接近全量、冲突 0。

**选择混淆矩阵**：行 = 应选技能，列 = agent 实际选中。对角线绿色是正确命中；行内出现红色数字 = 该 skill 的任务被列方向的 skill 抢走，颜色越深次数越多。

**冲突明细**：每条冲突给出任务原文、应选 vs 实际、票数一致率，以及**截胡关键词**——截胡者 description 里命中任务文本的词，这就是你要改的地方。

**修复闭环（已自动化）**：真实模式下，报告会自动生成「修复建议」区块——对每个冲突对给出 description 改前/改后对照与理由（优先给截胡方加"仅限/不含"边界词）。照改 → 重跑 → 看冲突是否消失。demo 中两处撞车都如此可修。mock 模式跳过此区块。

**三个判定规则**（方法学核心）：
1. **一致率 ≥ 0.8 才算稳定冲突**（5 次采样至少 4 次同结果）。低于此值标"不稳定"——那是模型随机性，不是真冲突，不要据此改 description。
2. **正向任务全绿是底线**。正向任务出现冲突说明 description 问题严重；冲突集中在灰区属正常现象，灰区本来就是设计来逼出边界问题的。
3. **NONE 也是信号**。agent 认为没有技能接得住——说明该能力缺 skill，或 description 没覆盖到这类问法。

---

## 4. 目录结构

```
skill-seam/
├── skill_seam.py          # 主脚本（scan → generate → simulate → report）
├── tests/test_atlas.py    # 98 项测试（unittest，纯标准库）
├── tests/web_smoke.cjs    # 103 项网页断言（node，无需依赖）
├── demo-skills/           # 眼科客服样例（中文），6 个 skill，埋 2 处撞车
├── demo-skills-en/        # 同一场景的英文样例，网页 ?lang=en 的演示数据来源
├── examples/              # 真实运行归档（报告/结果/任务，qwen3.8-flash；含英文 demo 一套）
├── README.md              # 快速上手
└── output/                # 每次运行覆盖写入 results.json + report.html
```

## 5. 已知局限（诚实清单）

- frontmatter 解析支持单行 `key: value`，不支持 YAML 多行折叠（`>-`）语法。
- 模拟环境是"复现式"的（同样的 name+description 注入格式），不是在真实 agent 进程里跑——跨 agent 的真实行为差异属于 v1.1 路线（本机驱动 codex/claude CLI）。
- **自动生成任务存在生成者偏见**（见 §2 实测警告）：生成问法对自身 description 的命中率系统性偏高，不能替代真实问法做冲突结论。
- 灰区对的 LLM 预筛基于单次判断，可能漏对（demo 中首轮词面相似度就漏了埋的撞车对）——用户补充灰区任务仍是必要的。
- mock 模式结论仅供验证管线，不可作为修复依据。

## 6. 路线图

- ~~v1.0（Phase 1）~~ 已完成：CLI 全链路（任务自动生成、修复建议、参数集、任务清单格式）+ **网页版全功能**（演示模式、BYOK 三端点直连、任务自动生成、修复建议、冲突热力图）。
- ~~事故标记（v1.1 第一项）~~ 已完成：`mark` 子命令 + 本地事故库（`~/.skill-seam/marked.jsonl`）+ `--with-marked` 合并（按当前技能集自动过滤适用条目）。
- ~~真实问法沉淀（v1.1 第二项）~~ 已完成：**`capture --install-claude`**（Claude Code `UserPromptSubmit` hook，被动收集每条 prompt 到 `~/.skill-seam/queries.jsonl`，静默零打扰）；**Codex 会话日志解析器**（宽容抽取 `~/.codex/sessions/` 里的 user 消息）；**`harvest` 收割命令**（清洗去重 → Dice 相关性排序 → 产出任务草稿，`--label` 可用 LLM 预标应选技能）。
- ~~对抗式灰区生成（v1.1 第三项）~~ 已完成：灰区出题改为"红队任务"框架（目标是写出让 agent 选错的问法），CLI 侧自动注入事故库真实问法做 few-shot 模仿真实分布；网页版同步红队 prompt。
- **v1.1 原有**：跨 agent 一致性——同一任务集在 Codex / Claude / Gemini 下的选择差异报告。
- **v1.2**：数据飞轮——opt-in 匿名收集冲突对 pattern，发布《Skill 干扰模式报告》。

### 为什么事故标记重要（方法学）

工具的核心价值依赖"真实用户问法"，而实测证明自动生成任务存在生成者偏见（demo 中真实问法 4 冲突 vs 生成任务 0 冲突）。`mark` 让真实失败在一条命令内变成永久测试资产——攒十次真实事故，任务库就比任何生成器都准。v1.1 的被动捕获会把这件事从"手动标记"升级为"使用中自动沉淀"。

### 产品形态定位

**网页版 = 个人用户的完整形态**：打开链接 → 看演示 → 粘自己的 skill → 自动生成任务 → 模拟 → 按修复建议改 → 网页内重跑验证。零安装，覆盖个人场景全部需求。
**CLI = 团队与自动化的引擎**：直读本地 skill 目录（无需粘贴）、CI 退出码门禁、大批量任务、跨 agent 批测。
两者的 SYSTEM_PROMPT、一致率阈值（0.8）、冲突判定规则保持同步（两份文件头部有互指注释），改动必须两边一起。
